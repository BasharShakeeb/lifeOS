Use the attached UI as the visual source of truth.

Do NOT redesign the interface.

Keep the layout, spacing, typography, colors, icons, sidebar, navbar, and component hierarchy exactly as shown.

This task is a complete UI refinement only.

Requirements:

- Remove all mock data, fake statistics, placeholder tasks, demo projects, sample cards, and hardcoded content.
- Keep only the UI structure and reusable components.
- Replace all displayed data with professional empty states.
- Do not connect to any backend.
- Do not use mock JSON, fake APIs, or demo datasets.

Typography

- Improve the typography across the entire application.
- Use one modern, clean Arabic font consistently across all pages.
- Apply a consistent font hierarchy for titles, subtitles, body text, labels, and buttons.
- Fix all font sizes, weights, spacing, and line heights.
- Ensure excellent readability and a premium SaaS appearance.

Cards

- Make every card in the application visually consistent.
- Use the same width, height, border radius, padding, shadows, spacing, and typography.
- Align all cards perfectly.
- Make card heights consistent whenever possible.
- Apply the same hover effect to every card.
- Keep all cards responsive.

Components

Create reusable components for:

- Sidebar
- Navbar
- Cards
- Statistics Cards
- Tables
- Filters
- Search
- Pagination
- Forms
- Drawers
- Dialogs
- Empty States
- Loading States

Navigation

Keep all page routing and navigation working.

Buttons should navigate between the existing pages only.

Do not implement CRUD logic yet.

State

Every page should start with an empty state ready for backend integration.

Tech Stack

- Next.js App Router
- TypeScript
- Tailwind CSS
- shadcn/ui
- Responsive Design
- Reusable Components

Goal

Prepare a clean, professional frontend that is fully ready for backend integration later without changing the UI or visual identity.