إنشاء نموذج موحد (Universal Form)

أريد إعادة تصميم نظام النماذج بالكامل في LifeOS.

لا أريد نموذجًا منفصلًا لكل قسم، بل أريد إنشاء Universal Form System يمكن إعادة استخدامه في جميع وحدات التطبيق (Tasks, Habits, Goals, Assignments, Events, Notes, Finance, وغيرها).

المتطلبات
إنشاء نظام Forms قابل لإعادة الاستخدام (Reusable).
جميع الوحدات تستخدم نفس مكونات الإدخال (Input Components).
إنشاء مكتبة مكونات مشتركة تشمل:
Text Input
Textarea
Select
Multi Select
Date Picker
Time Picker
DateTime Picker
Priority Selector
Tags
Category
Project / Workspace Selector
Reminder Picker
File Upload
Color Picker
Icon Picker
Repeat Settings
Subtasks
Rich Text Editor

أريد تحسين نموذج "إنشاء مهمة جديدة" ليكون بمستوى التطبيقاتالاحترافية مثل Todoist وClickUp وNotion، مع الحفاظ على التصميم البسيطوعدم ازدحام الواجهة.

المتطلبات

إضافة حقل المشروع (Project / Workspace).

إضافة حقل الفئة (Category).

إضافة الوسوم (Tags) مع إمكانية إضافة أكثر من وسم.

دعم تاريخ ووقت الاستحقاق بدلًا من التاريخ فقط.

إضافة التذكيرات (Reminders) مع خيارات مثل: قبل 10 دقائق، ساعة،يوم.

إضافة المهام الفرعية (Subtasks) مع إمكانية الإضافة والحذفبسهولة.

دعم المرفقات (صور، PDF، ملفات).

إضافة تكرار المهمة (يومي، أسبوعي، شهري، مخصص).

تحسين اختيار الأولوية باستخدام ألوان واضحة (منخفضة، متوسطة،عالية، عاجلة).

إضافة حالة المهمة (To Do، In Progress، Completed).

إضافة زرين:

حفظ

حفظ وإنشاء مهمة أخرى

التحقق من صحة الحقول وإظهار رسائل خطأ واضحة.

جعل النموذج متجاوبًا مع الهاتف والكمبيوتر.

استخدام مكونات shadcn/ui مع Tailwind CSS والحفاظ على هويةوتصميم LifeOS.

إضافة انتقالات وحركات بسيطة باستخدام Framer Motion.

ربط جميع الحقول بقاعدة بيانات Supabase مع احترام Row LevelSecurity (RLS).

تنظيم الكود بحيث يكون قابلًا للتوسعة والصيانة، مع فصل مكونات النموذجإلى مكونات مستقلة.

الهدف
انتقالات سلسة باستخدام Framer Motion.
التحقق من صحة الحقول وإظهار رسائل خطأ واضحة.

أريد نموذجًا احترافيًا وسريع الاستخدام ينافس التطبيقات العالمية، مع أفضلتجربة مستخدم (UX) وتصميم عصري (UI).